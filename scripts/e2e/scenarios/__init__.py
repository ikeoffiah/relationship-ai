"""Scripted conversations replayed against the running stack.

See ``docs/intelligence-test-plan.md``. Each module here holds one group from
§3 of that plan; ``runner.py`` holds the machinery they share.

The thing these test that a unit test cannot is *silence* — that twelve turns
of ordinary life produce no caution, no coaching and no nudge. There is no
natural home for that assertion anywhere else, and it is the single most
important behaviour in the product.
"""
